image: alpine:latest
kernel: Linux 7.0.14-orbstack-00380-ga7e0a2dc9535 #1 SMP PREEMPT Fri Aug  7 03:48:40 UTC 2026 aarch64
docker: 29.4.0 linux/arm64
recorded: 2026-09-25T04:14:28Z
native-arch: aarch64
translated: every other architecture runs through the Docker host's binfmt_misc handler
binfmt: mac-macho-arm64 enabled interpreter [mac] 
binfmt: mac-macho-x86_64 enabled interpreter [mac] 
binfmt: mac-universal-arm64 enabled interpreter [mac] 
binfmt: mac-universal-x86_64 enabled interpreter [mac] 
binfmt: qemu-aarch64 disabled interpreter [qemu] 
binfmt: qemu-arm enabled interpreter [qemu-arm] 
binfmt: qemu-i386 enabled interpreter [qemu-i386] 
binfmt: qemu-mips64el enabled interpreter [qemu-mips64el] 
binfmt: qemu-ppc64le enabled interpreter [qemu-ppc64le] 
binfmt: qemu-riscv64 enabled interpreter [qemu-riscv64] 
binfmt: qemu-s390x enabled interpreter [qemu-s390x] 
binfmt: qemu-x86_64 enabled interpreter [qemu] 
binfmt: rosetta enabled interpreter [rosetta] 
binfmt: rosettaM-bM-^@M-^K enabled interpreter [rosetta] 
binfmt: rosettaM-bM-^@M-^KM-bM-^@M-^K enabled interpreter [rosetta] 
seccomp: unconfined
override: riscv64/mman from aarch64 (mm/madvise.c and mm/mprotect.c are generic; QEMU 'target_madvise' ignores most advice and QEMU validates mprotect's prot before its length)
override: x86_64/signals run under qemu-x86_64 version 11.0.3 (Rosetta resets an SA_RESETHAND disposition before the handler runs, so the process dies of SIGUSR2; get_signal in kernel/signal.c is architecture-independent and the native aarch64 run shows the handler running once)
override: x86_64/threads from aarch64 (Rosetta returns ENOSYS for clone3 and futex_waitv; the checked behavior is in architecture-independent kernel/fork.c, kernel/futex/, kernel/signal.c, and fs/proc/base.c, and the native aarch64 run passes every check)
override: riscv64/threads from aarch64 (qemu-riscv64 returns ENOSYS for clone3, futex_waitv, and the robust-list calls, and its /proc/self/task lists the emulator's own threads; the checked behavior is in architecture-independent kernel/fork.c, kernel/futex/, kernel/signal.c, and fs/proc/base.c)
override: x86_64/exec from aarch64 (Rosetta runs executed programs through binfmt_misc, so AT_EXECFN and comm name the translator and the #! stage dies of SIGTRAP; fs/exec.c and fs/binfmt_script.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/exec from aarch64 (qemu-riscv64 runs executed programs through binfmt_misc, so AT_EXECFN and comm name the translator and it cannot load a #! script's interpreter; fs/exec.c and fs/binfmt_script.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/fork from aarch64 (qemu-riscv64 gives clone children SIGCHLD as their exit signal whatever was requested and raises SIGCHLD for stops despite SA_NOCLDSTOP; kernel/fork.c, kernel/exit.c, and kernel/signal.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/events from aarch64 (qemu-riscv64 does not translate TFD_IOC_SET_TICKS, does not check signalfd4's mask size, and hands out its own POSIX timer IDs (so a default timer's si_value is not the kernel's ID); fs/timerfd.c, fs/signalfd.c, and kernel/time/posix-timers.c are architecture-independent and the native aarch64 run passes every check)
override: x86_64/epoll from aarch64 (Rosetta converts the packed x86-64 struct epoll_event itself and dies of SIGSEGV on a bad event pointer instead of returning EFAULT; fs/eventpoll.c is architecture-independent (the x86-64 layout is unit-tested) and the native aarch64 run passes every check)
override: riscv64/epoll from aarch64 (qemu-riscv64 does not apply epoll_pwait's signal mask for the wait, so the timer signal interrupts it; fs/eventpoll.c is architecture-independent and the native aarch64 run passes every check)
override: riscv64/sockets from aarch64 (qemu-riscv64 drops unknown socket type flag bits (so socket(AF_INET, 99, 0) and SOCK_STREAM | 0x100 are not EINVAL) and writes socketpair's descriptors only on success; net/socket.c is architecture-independent and the native aarch64 run passes every check)
override: riscv64/sockmsg from aarch64 (qemu-riscv64 ignores recvmmsg's timeout argument, so the invalid timeout is not refused and the call blocks; net/socket.c is architecture-independent and the native aarch64 run passes every check)
override: x86_64/shmem from aarch64 (Rosetta dies of SIGTRAP on mremap with an old length of 0 (duplicating a shared mapping); mm/mremap.c and mm/madvise.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/shmem from aarch64 (qemu-riscv64 refuses mremap's zero old length with EINVAL before checking MREMAP_MAYMOVE and does not implement MADV_REMOVE; mm/mremap.c and mm/madvise.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/memfd from aarch64 (qemu-riscv64 does not implement MADV_REMOVE (it returns 0), so neither the access check nor the seal check refuses it; mm/madvise.c and mm/shmem.c are architecture-independent and the native aarch64 run passes every check)
override: x86_64/pidfd from aarch64 (Rosetta returns ENOSYS for pidfd_getfd and clone3; kernel/pid.c, fs/pidfs.c, kernel/fork.c, kernel/signal.c, and kernel/exit.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/pidfd from aarch64 (qemu-riscv64 does not translate PIDFD_GET_INFO (ENOTTY) or FS_IOC_GETVERSION, writes waitid's siginfo when the call fails, returns ENOSYS for clone3, and accepts CLONE_DETACHED with CLONE_PIDFD; kernel/pid.c, fs/pidfs.c, kernel/fork.c, kernel/signal.c, and kernel/exit.c are architecture-independent and the native aarch64 run passes every check)
override: x86_64/fdinfo from aarch64 (fdinfo shows the arm64 host kernel's own status-flag encoding (O_LARGEFILE, O_DIRECTORY), which Rosetta does not convert as it converts F_GETFL; fs/proc/fd.c and the files' show_fdinfo operations are architecture-independent and the native aarch64 run passes every check)
override: riscv64/fdinfo from aarch64 (fdinfo shows the arm64 host kernel's own status-flag encoding (O_LARGEFILE, O_DIRECTORY), which qemu-riscv64 does not convert as it converts F_GETFL; fs/proc/fd.c and the files' show_fdinfo operations are architecture-independent and the native aarch64 run passes every check)
override: x86_64/nodes from aarch64 (Rosetta dies of SIGSEGV on utime with an unmapped struct utimbuf instead of returning EFAULT (the other raw x86-64 time calls pass under it); fs/namei.c and fs/utimes.c are architecture-independent and the native aarch64 run passes every check)
override: x86_64/xattr from aarch64 (Rosetta dies of SIGSEGV on getxattr with an unmapped path instead of returning EFAULT; fs/xattr.c and the file systems' handlers are architecture-independent and the native aarch64 run passes every check)
override: riscv64/xattr from aarch64 (qemu-riscv64 returns ENOSYS for the *xattrat calls and faults copying a zero-length value to an unmapped buffer (the kernel copies nothing); fs/xattr.c is architecture-independent and the native aarch64 run passes every check)
override: riscv64/netlink from aarch64 (qemu-riscv64 translates netlink messages itself: it drops a request of an unknown type (past RTM_MAX) instead of passing it to the kernel, refuses a zero-length SOL_NETLINK option with EINVAL, and answers an unknown one with EOPNOTSUPP; net/netlink/af_netlink.c and net/core/rtnetlink.c are architecture-independent and the native aarch64 run passes every check)
override: x86_64/sysvshm from aarch64 (Rosetta returns 1 rather than -1 with EINVAL or EACCES from a failed shmat (an unknown or negative identifier, a segment the caller may not use); ipc/shm.c and ipc/util.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/sysvshm from aarch64 (qemu-riscv64 converts struct ipc64_perm and shmid64_ds itself, reporting a new or removed segment's status differently from the kernel, and refuses IPC_SET with EINVAL; ipc/shm.c and ipc/util.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/sysvmsg from aarch64 (qemu-riscv64 converts struct msqid64_ds itself, reporting a queue's status differently from the kernel; ipc/msg.c and ipc/util.c are architecture-independent and the native aarch64 run passes every check)
override: x86_64/seccomp from aarch64 (Rosetta refuses seccomp (the system call and PR_GET_SECCOMP and PR_SET_SECCOMP fail with EINVAL); kernel/seccomp.c and the classic-to-eBPF conversion in net/core/filter.c are architecture-independent, the checks compare against the target's own AUDIT_ARCH and system-call numbers, and the native aarch64 run passes every check)
override: riscv64/seccomp from aarch64 (qemu-riscv64 refuses seccomp (the system call is ENOSYS, PR_GET_SECCOMP and PR_SET_SECCOMP EINVAL); kernel/seccomp.c and the classic-to-eBPF conversion in net/core/filter.c are architecture-independent, the checks compare against the target's own AUDIT_ARCH and system-call numbers, and the native aarch64 run passes every check)
override: riscv64/inotify from aarch64 (qemu-riscv64 does not translate INOTIFY_IOC_SETNEXTWD (ENOTTY); fs/notify/inotify/inotify_user.c is architecture-independent and the native aarch64 run passes every check)
override: x86_64/admin from aarch64 (Rosetta returns ENOSYS for mount_setattr and open_tree_attr; fs/namespace.c, fs/fsopen.c, and the other calls' kernel code are architecture-independent and the native aarch64 run passes every check)
override: riscv64/admin from aarch64 (qemu-riscv64 reads pointer arguments before the kernel's checks (so they fault before the capability check), makes settimeofday succeed without setting the clock, does not write back a refused adjtimex, and returns ENOSYS for the module calls, fsopen, fspick, fsmount, fsconfig, mount_setattr, and open_tree_attr; fs/namespace.c, fs/fsopen.c, and the other calls' kernel code are architecture-independent and the native aarch64 run passes every check)
override: riscv64/mqueue from aarch64 (qemu-riscv64 emulates the message queue calls itself: it mishandles queue names (so an empty name opens a queue and every other name is ENOENT) and lacks mq_notify (ENOSYS); ipc/mqueue.c is architecture-independent and the native aarch64 run passes every check)
override: riscv64/sched from aarch64 (qemu-riscv64 converts struct sched_attr and the sched_setscheduler arguments itself (reading them before the kernel checks the call, and dropping the size checks) and its own threads join the process, so the thread, group, and /proc checks see them; kernel/sched/syscalls.c, kernel/sys.c, and block/ioprio.c are architecture-independent and the native aarch64 run passes every check)
override: x86_64/procmem from aarch64 (Rosetta returns ENOSYS for process_madvise; mm/madvise.c, mm/process_vm_access.c, and mm/gup.c are architecture-independent, the translated run passes every other check, and the native aarch64 run passes every check)
override: riscv64/procmem from aarch64 (qemu-riscv64 returns ENOSYS for process_vm_readv and process_vm_writev and does not refuse MADV_POPULATE_READ on a mapping without PROT_READ; mm/process_vm_access.c, mm/gup.c, and mm/madvise.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/kcmp from aarch64 (qemu-riscv64 cannot run a bare clone with CLONE_THREAD and no new TLS (the thread checks fail and the case hangs waiting for the threads); kernel/kcmp.c, kernel/fork.c, and block/blk-ioc.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/iovec from aarch64 (qemu-riscv64 converts vector arrays itself: it transfers the vectors before one it cannot reach instead of refusing the call, checks a single vector at its full length, and refuses a count past UIO_MAXIOV before truncating it to an unsigned int; lib/iov_iter.c and fs/read_write.c are architecture-independent and the native aarch64 run passes every check)
override: x86_64/mlock from aarch64 (Rosetta cannot run a process whose own memory mlockall(MCL_FUTURE) locks within a small RLIMIT_MEMLOCK (the translated fixture dies at its fork); mm/mlock.c, mm/mmap.c, and mm/mremap.c are architecture-independent and the native aarch64 run passes every check)
override: riscv64/mlock from aarch64 (qemu-riscv64 lacks mlock2 (ENOSYS), reports ENOMEM where the kernel refuses MAP_LOCKED with EPERM and a locked mremap with EAGAIN, and ignores MADV_FREE, MADV_COLD, and MADV_DONTNEED_LOCKED; mm/mlock.c, mm/mmap.c, mm/mremap.c, and mm/madvise.c are architecture-independent and the native aarch64 run passes every check)
