# Primary push/PR gate for rax.
#
# Breadth over every GitHub-hosted OS x CPU-architecture combination that can run
# rax natively. The core matrix covers 64-bit Linux and macOS. Windows C API
# build/runtime coverage runs below and in capi-release.yml; the patched vm-memory
# dependency supports Windows, but the full ISA/JIT suite is not admitted here.
# Each core platform builds
# *all* targets (lib, bins, examples, and every one of the ~34 integration-test
# binaries, including the ~1 MB generated tables) and then runs a fast,
# representative core slice. The exhaustive 124k-test run lives in
# full-suite.yml; the many-ISA build sweep in cross.yml.
#
# Feature gating is deliberate and load-bearing:
#   * `kvm`      is Linux-only (deps + cfg) and needs /dev/kvm at runtime, so the
#                portable lane drops it; a dedicated job exercises it (kvm.yml).
#   * `smir-jit` pulls the W^X exec runtime; enabled on Linux (x86_64+aarch64)
#                and Apple-Silicon macOS. This portable matrix still omits it
#                on Intel macOS; targeted Mach-O JIT runs are separate coverage.
#   * `x86_64-suite` is always on so the gated x86_64 test binaries compile.
name: CI

on:
  push:
    branches: [master, main]
  pull_request:
  workflow_dispatch:

# Newer pushes to the same branch/PR cancel older in-flight runs.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  # ---------------------------------------------------------------------------
  # Format + lints. Uses the full (default) Linux feature set incl. kvm/smir-jit
  # so clippy sees the most code paths.
  # ---------------------------------------------------------------------------
  lint:
    name: rustfmt + clippy
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: stable
          components: rustfmt, clippy
          cache-key: lint
      # Formatting and Clippy are required checks. Keep Clippy at the repository's
      # canonical warning policy; rustc warnings remain visible without promoting
      # every warning to an error via `-D warnings`.
      - name: rustfmt --check
        run: cargo fmt --all --check
      - name: clippy (all targets, default features)
        run: cargo clippy --all-targets --features x86_64-suite

  # ---------------------------------------------------------------------------
  # Compile everything on every native platform. This is the breadth guarantee:
  # the whole crate + all test binaries must *build* on each OS/arch even if the
  # full suite only *runs* on a subset.
  # ---------------------------------------------------------------------------
  build:
    name: build (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 75
    strategy:
      fail-fast: false
      matrix:
        include:
          - { name: linux-x64,   os: ubuntu-24.04,     features: "--no-default-features --features x86_64-suite,smir-jit" }
          - { name: linux-arm64, os: ubuntu-24.04-arm, features: "--no-default-features --features x86_64-suite,smir-jit" }
          - { name: macos-intel, os: macos-15-intel,   features: "--no-default-features --features x86_64-suite" } # portable CI selection; targeted Mach-O JIT runs are separate
          - { name: macos-arm64, os: macos-15,         features: "--no-default-features --features x86_64-suite,smir-jit" }
          # Windows C API is tested in capi-release.yml; these full-suite lanes
          # remain unvalidated (including host-specific test/assembler code).
          # - { name: windows-x64,   os: windows-2025,   features: "--no-default-features --features x86_64-suite" }
          # - { name: windows-arm64, os: windows-11-arm, features: "--no-default-features --features x86_64-suite" }
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: stable
          cache-key: build-${{ matrix.name }}
      - name: cargo build --all-targets
        run: cargo build --all-targets ${{ matrix.features }}

  # ---------------------------------------------------------------------------
  # Run a fast, cross-cutting core slice on every native platform. Picks small-
  # to-medium binaries that touch x86_64, ARM, RISC-V, SMIR, and the boot paths,
  # but skips the multi-hundred-KB generated diff tables (those run in
  # full-suite.yml). The emulator is host-independent, so e.g. the x86_64 suite
  # is meaningful even on an arm64 host. (realmode_boot is gated to x86_64 hosts,
  # so it is an empty no-op on the arm64 lanes; riscv_boot covers a boot path on
  # every platform.)
  # ---------------------------------------------------------------------------
  test-core:
    name: test-core (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - { name: linux-x64,   os: ubuntu-24.04,     features: "--no-default-features --features x86_64-suite,smir-jit" }
          - { name: linux-arm64, os: ubuntu-24.04-arm, features: "--no-default-features --features x86_64-suite,smir-jit" }
          - { name: macos-intel, os: macos-15-intel,   features: "--no-default-features --features x86_64-suite" } # portable CI selection; targeted Mach-O JIT runs are separate
          - { name: macos-arm64, os: macos-15,         features: "--no-default-features --features x86_64-suite,smir-jit" }
          # Windows C API is tested separately in capi-release.yml.
          # - { name: windows-x64,   os: windows-2025,   features: "--no-default-features --features x86_64-suite" }
          # - { name: windows-arm64, os: windows-11-arm, features: "--no-default-features --features x86_64-suite" }
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: stable
          cache-key: test-core-${{ matrix.name }}
      # The library target contains native replay differentials that map and
      # execute raw host instructions. Serialize every selected test binary so
      # a native signal or state mismatch is attributable to its exact test;
      # nocapture preserves earlier assertion context if a later signal aborts
      # the process.
      - name: cargo test (core slice)
        shell: bash
        run: |
          cargo test ${{ matrix.features }} \
            --lib \
            --test x86_64 \
            --test asm_instructions \
            --test arm \
            --test realmode_boot \
            --test riscv_boot \
            --test riscv_smir_x86_jit \
            --test riscv_smir_aarch64_jit \
            --test smir_avx10_roundtrip \
            --test smir_jit_aarch32_aarch64 \
            --test smir_jit_thumb_aarch64 \
            --test smir_jit_x86_aarch64 \
            --test smir_jit_evex_masking \
            --test x86_64_unimplemented_source_inventory \
            --test isa_oracle \
            -- --include-ignored --test-threads=1 --nocapture
      # The x86-64 JIT suite executes transient native mappings and intentionally
      # exercises host feature-specific encodings. Run it serially so a native
      # signal is attributable to one test and concurrent JIT executions cannot
      # interfere with this correctness gate. Other matrix hosts either compile
      # this target in the build job or cfg it to an empty test binary.
      - name: cargo test (native x86-64 JIT, serial)
        if: matrix.name == 'linux-x64'
        shell: bash
        env:
          # Preserve assertion output when a later native signal terminates the
          # test process, and arm the async-signal-safe JIT crash diagnostic.
          RAX_JIT_TRACE: "1"
        run: |
          cargo test ${{ matrix.features }} \
            --test smir_jit_vcpu \
            -- --include-ignored --test-threads=1 --nocapture

  # Every push must exercise the embedding API and installed C/C++ consumers
  # natively on all supported operating systems, including both link modes.
  test-capi:
    name: test-capi (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - { name: linux-x64, os: ubuntu-24.04 }
          - { name: macos-arm64, os: macos-15 }
          - { name: windows-x64, os: windows-2022 }
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: stable
          cache-key: test-capi-${{ matrix.name }}
      - name: C API semantic regressions
        run: cargo test --locked -p rax-capi --no-default-features
      - name: Configure native SDK
        run: cmake -S capi -B capi-build -DRAX_CARGO_PROFILE=dev -DCMAKE_INSTALL_PREFIX=${{ github.workspace }}/capi-install
      - name: Build native SDK
        run: cmake --build capi-build --config Release
      - name: Install SDK
        run: cmake --install capi-build --config Release
      - name: Configure installed consumers
        run: cmake -S capi/tests/consumer -B capi-consumers -DCMAKE_PREFIX_PATH=${{ github.workspace }}/capi-install
      - name: Compile C ABI and C++ consumers
        run: cmake --build capi-consumers --config Release
      - name: Run shared and static consumers
        run: ctest --test-dir capi-consumers -C Release --output-on-failure --no-tests=error

  # Aggregate gate: a single required check that depends on the whole matrix.
  ci-success:
    name: CI success
    if: always()
    needs: [lint, build, test-core, test-capi]
    runs-on: ubuntu-24.04
    steps:
      - name: Verify all matrix jobs succeeded
        run: |
          if [ "${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') || contains(needs.*.result, 'skipped') }}" = "true" ]; then
            echo "One or more required jobs failed."
            exit 1
          fi
          echo "All required jobs passed."
